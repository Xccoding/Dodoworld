
function DodoWorld:OnPlayerPickHero( params )
    local unit = EntIndexToHScript(params.heroindex)
    -- for key, value in pairs(params) do
    --     print(key, value)
    -- end
    -- CustomNetTables:SetTableValue("hero_schools", tostring(unit:GetPlayerOwnerID()), {schools_index = 0})
end